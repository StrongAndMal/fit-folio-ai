
import React from "react";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

const Settings = () => (
  <Card>
    <CardHeader>
      <CardTitle>Settings & Subscription</CardTitle>
    </CardHeader>
    <CardContent>
      <div>Manage your account, plan, and notifications here.</div>
    </CardContent>
  </Card>
);

export default Settings;
