
import React from "react";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

const TeamScore = () => (
  <Card>
    <CardHeader>
      <CardTitle>AI Team Template Score</CardTitle>
    </CardHeader>
    <CardContent>
      <div>Your AI fitness/form analysis and feedback will appear here.</div>
    </CardContent>
  </Card>
);

export default TeamScore;
