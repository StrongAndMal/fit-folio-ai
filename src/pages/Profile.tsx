
import React from "react";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

const Profile = () => (
  <Card>
    <CardHeader>
      <CardTitle>Your Profile</CardTitle>
    </CardHeader>
    <CardContent>
      <div>Manage your profile, stats, and info here.</div>
    </CardContent>
  </Card>
);

export default Profile;
